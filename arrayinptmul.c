#include<stdio.h>
   int main(){

   int ar[10],n,i,mul=1;
   scanf(" %d ", &n);

   for(i=1;i<=n;i++){

    scanf(" %d ",&a[i]);
    mul = mul * a[i];
   }

   printf(" %d ", mul);
   return 0;

   }
