#include<stdio.h>
  int main(){

  int i, n, a[], smallest;
  scanf("%d",&n);
  for (i=0;i<n;i++){

    scanf("%d", &a[]);
  }

  smallest = a[0];
  for(i=0;i<n;i++){

    if(a[i]<smallest){

        smallest = a[i];
    }
  }
  printf("%d ", smallest);
  return 0;

  }
